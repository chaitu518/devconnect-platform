server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devconnect_user
    username: root
    password: root

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: user-service-group
      auto-offset-reset: earliest



  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
